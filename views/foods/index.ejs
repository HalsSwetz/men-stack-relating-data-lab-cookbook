
<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>My Pantry</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1 class="pantry-header">Welcome to my Pantry</h1>
    
    
    <% if (foods.length === 0) { %>
        <p>There are no items in this pantry.</p>
    <% } else { %>
       <ul class="food-list">
            <% foods.forEach(food => { %>
            <li>
                <a href="/users/<%= user._id %>/foods/<%= food._id %>">
                <%= food.name %>
                </a>
            </li>
        <% }) %>
       </ul>
    <% } %>

    <a class="add-item" href="/users/<%= user._id %>/foods/new" >Add an item to your Pantry</a>
</body>
</html>
